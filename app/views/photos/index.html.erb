<%= link_to "Upload new photo", new_photo_path, class: "btn btn-info" %>

<% if @photos == [] %>
<p>Photo ninchen</p>
<% else %>
  <% @photos.each do |photo| %>
  <br><%= image_tag(photo.photo_url) %></br>
    <br><%= photo.name %></br>
  <% end %>
<% end %>